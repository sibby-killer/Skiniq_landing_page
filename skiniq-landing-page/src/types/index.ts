export type Benefit = {
  icon: React.ElementType;
  title: string;
  description: string;
};

export type Problem = {
  icon: React.ElementType;
  title: string;
  description: string;
  stat: string;
};

export type Outcome = {
  icon: React.ElementType;
  title: string;
  description: string;
};

export type Step = {
  step: string;
  title: string;
  description: string;
};

export type Feature = {
  icon: React.ElementType;
  title: string;
  description: string;
};

export type Testimonial = {
  name: string;
  quote: string;
};